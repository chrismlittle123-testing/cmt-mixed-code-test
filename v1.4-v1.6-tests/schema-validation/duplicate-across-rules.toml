# Test schema validation for duplicate extensions across rules
# This should FAIL validation

[code.naming]
enabled = true

[[code.naming.rules]]
extensions = ["ts", "tsx"]
file_case = "kebab-case"
folder_case = "kebab-case"

[[code.naming.rules]]
extensions = ["ts", "js"]  # "ts" is duplicate from rule 1
file_case = "snake_case"
folder_case = "snake_case"
